<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>URL Shortener</title>
    <!-- Bootstrap CSS -->
    <link
      href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <!-- Custom CSS -->
    <style>
      /* Custom styles */
      body {
        background-color: #f8f9fa;
        font-family: Arial, sans-serif;
        margin-bottom: 100px; /* Adjusted margin for the footer */
      }

      .navbar-brand {
        font-size: 1.2rem; /* Adjusted font size */
      }

      .navbar-nav .nav-item .nav-link {
        font-size: 1rem; /* Adjusted font size */
      }

      .jumbotron {
        background-color: #343a40;
        color: #ffffff;
        padding: 2rem 1rem; /* Adjusted padding */
        margin-bottom: 0;
      }

      .jumbotron h1 {
        font-size: 2rem; /* Adjusted font size */
      }

      .jumbotron p {
        font-size: 1rem; /* Adjusted font size */
      }

      .card {
        border: none;
        border-radius: 0;
        box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.1);
      }

      .card-body {
        padding: 2rem;
      }

      .form-control {
        border-radius: 0;
        border: 1px solid #ced4da;
        transition: border-color 0.15s ease-in-out, box-shadow 0.15s ease-in-out;
      }

      .form-control:focus {
        border-color: #007bff;
        box-shadow: 0 0 0 0.2rem rgba(0, 123, 255, 0.25);
      }

      .btn-primary {
        border-radius: 0;
        background-color: #007bff;
        border: none;
      }

      .btn-primary:hover {
        background-color: #0056b3;
      }

      .footer {
        position: fixed;
        bottom: 0;
        width: 100%;
        background-color: #343a40;
        color: #ffffff;
        padding: 1rem 0; /* Adjusted padding */
        text-align: center;
        z-index: 1000; /* Added z-index to ensure it stays on top */
      }
    </style>
  </head>

  <body>
    <!-- Header -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
      <div class="container">
        <a class="navbar-brand" href="/"><i class="fas fa-cut"></i> Scissors</a>
        <button
          class="navbar-toggler"
          type="button"
          data-toggle="collapse"
          data-target="#navbarNav"
          aria-controls="navbarNav"
          aria-expanded="false"
          aria-label="Toggle navigation"
        >
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav ml-auto">
            <li class="nav-item">
              <a class="nav-link" href="/shorturl/login">Login</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/shorturl/register">Sign Up</a>
            </li>
          </ul>
        </div>
      </div>
    </nav>

    <!-- Jumbotron -->
    <div class="jumbotron text-center">
      <h1 class="display-4">Scissors</h1>
      <p class="lead">Brief is the new black!</p>
    </div>

    <!-- Main Content -->
    <div class="container">
      <div class="row justify-content-center">
        <div class="col-md-8">
          <div class="card">
            <div class="card-body">
              <h2 class="text-center mb-4">Why Choose Us?</h2>
              <p class="text-center">Our URL Shortener service offers:</p>
              <ul>
                <li>Quick and easy URL shortening</li>
                <li>Customizable shortened URLs</li>
                <li>Trackable click analytics</li>
                <li>User-friendly interface</li>
              </ul>
            </div>
          </div>
        </div>
      </div>
    </div>

    <!-- Footer -->
    <div class="footer">
      <p>&copy; 2024 URL Shortener</p>
    </div>

    <!-- Bootstrap JS (optional) -->
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.5.4/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
  </body>
</html>
